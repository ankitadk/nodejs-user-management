<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  </head>
  <body>
    <div class="border-b py-3">
      <div class="flex justify-between max-w-5xl mx-auto items-center">
        <a href="/" class="text-lg">User Management</a>
        <a href="/add" class="hover:text-blue-600 text-sm">Add User</a>
      </div>
    </div>

    <div class="mt-16 mx-auto w-[400px]">
      <% if(usersCount == 0) { %>
      <h1 class="text-lg font-bold">No users avaialble!</h1>
      <p class="text-sm">
        Lorem ipsum dolor sit amet, consectetur adipisicing elit. Sunt deserunt
        libero eveniet.
      </p>
      <a href="/add" class="bg-black text-white px-4 py-2 mt-2 inline-block"
        >Add User</a
      >
      <%} else {%>
      <h1 class="text-lg font-bold">Manage Users</h1>
      <p class="text-sm">
        Lorem ipsum dolor sit, amet consectetur adipisicing elit. Obcaecati ut
        sint nulla!
      </p>

      <% for(let i=0; i<users.length; i++) {%>
        <div class="mt-3 flex justify-between items-center">
            <div class="flex gap-2 items-center">
                <img class="w-10 h-10 object-cover rounded-full" src=<%=users[i].img%> alt="">
                <div class="text-sm"><%=users[i].name%></div>
                <div class="text-sm"><%=users[i].email%></div>
            </div>
            <div class="flex gap-3 items-center">
                <form action="/edit" method="post" class="inline">
                    <input type="hidden" name="userId" value="<%=i%>">
                    <button type="submit"><i class="bi bi-pencil"></i></button>
                </form>
                <form action="/delete" method="post" class="inline">
                    <input type="hidden" name="userId" value="<%=i%>">
                    <button type="submit"><i class="bi bi-trash3"></i></button>
                </form>
            </div>
        </div>
      <%}%>
      <%} %>
    </div>
  </body>
</html>
